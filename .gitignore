# forge-std is fetched with `forge install --no-git`, not vendored — same as the
# fork-in-hood repo. .gitmodules is listed too, so that an install without
# --no-git still leaves the tree tidy: the checkout is a build input, not source.
lib/
.gitmodules
out/
cache/
broadcast/
foundry.lock
.DS_Store
